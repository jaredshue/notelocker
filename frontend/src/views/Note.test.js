describe("testing Note.js",() => {

    test("given GUID, fetch the right note",() => {
        const givenGUID = [
            {id:1, url:/notes/1},
            {id:2, url:/notes/2},
        ]
        const outputNote = [];
        // expect 
        
    });


});